# Configuração de Email SMTP
# Este arquivo deve ser copiado para .env e preenchido com suas credenciais reais

# Servidor SMTP
# Gmail: smtp.gmail.com (porta 587 com TLS ou 465 com SSL)
# Mailtrap: smtp.mailtrap.io (porta 465 com SSL)
# Outro serviço: consulte a documentação
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASSWORD=sua-senha-ou-app-password
SMTP_FROM_EMAIL=seu-email@gmail.com
SMTP_USE_TLS=true

# URL do frontend (para gerar links de reset de senha)
FRONTEND_URL=http://localhost:5174

# Nota de Segurança:
# - Use variáveis de ambiente, NUNCA faça commit de credenciais reais
# - Para Gmail, gere uma "App Password" em https://myaccount.google.com/apppasswords
# - Para testes, você pode usar Mailtrap (serviço gratuito para testes)
