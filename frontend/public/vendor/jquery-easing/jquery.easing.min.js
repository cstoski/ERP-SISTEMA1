/* Minimal jquery-easing stub to avoid 404. Replace with full library if needed. */
(function (global) {
  if (!global.jQuery) {
    return;
  }
  if (!global.jQuery.easing) {
    global.jQuery.easing = {};
  }
})(typeof window !== 'undefined' ? window : this);
