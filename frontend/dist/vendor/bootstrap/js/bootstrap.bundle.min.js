/* Minimal bootstrap bundle stub to avoid 404. Replace with full bundle if needed. */
(function (global) {
  if (!global.bootstrap) {
    global.bootstrap = {};
  }
})(typeof window !== 'undefined' ? window : this);
