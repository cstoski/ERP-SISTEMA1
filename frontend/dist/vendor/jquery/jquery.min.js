/* Minimal jQuery stub to avoid 404. Replace with full jQuery if needed. */
(function (global) {
  if (global.jQuery) {
    return;
  }
  var stub = function () {
    return stub;
  };
  stub.fn = stub.prototype = {
    ready: function (cb) {
      if (typeof cb === 'function') {
        cb();
      }
      return stub;
    }
  };
  global.jQuery = global.$ = stub;
})(typeof window !== 'undefined' ? window : this);
